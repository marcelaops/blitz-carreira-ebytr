/* eslint-disable mocha/no-mocha-arrows */
require('dotenv').config();

// const mongoDbUrl = `mongodb://${process.env.HOST || 'mongodb'}:27017/ToDoList`;
// const url = 'http://localhost:3000';

describe('Insira uma nova tarefa na lista', () => {
  beforeAll(async () => {

  });
});
